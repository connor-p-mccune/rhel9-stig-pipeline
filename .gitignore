# Terraform
*.tfstate
*.tfstate.backup
.terraform/
*.tfvars

# .terraform.lock.hcl is intentionally NOT ignored - it should be committed

# Packer
packer_cache/
*-manifest.json

# Secrets and credentials
*.pem
*.key
.env
